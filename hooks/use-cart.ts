// This file is intentionally left empty or contains only a re-export
// if useCart is defined in context/cart-context.tsx.
// If you want to keep a separate hook file, you can re-export like this:
export { useCart } from "@/context/cart-context"
